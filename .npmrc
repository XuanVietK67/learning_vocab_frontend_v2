# Flat, hoisted node_modules — maximizes compatibility for both Next.js and
# Expo/Metro (Metro does not resolve pnpm's symlinked store well). Keep this
# until there's a concrete reason to switch to isolated linking.
node-linker=hoisted
